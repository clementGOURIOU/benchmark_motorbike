
  fieldMinMax
  {
    type                fieldMinMax;
    libs  ("libfieldFunctionObjects.so");
    log                 yes;
    writeControl       timeStep;
    writeInterval      1;
    //mode                magnitude;
    fields              ( U p T);
  }
